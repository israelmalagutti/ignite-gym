export type ExerciseDTO = {
  id: string;

  group: string;
  name: string;

  repetitions: number;
  series: number;

  demo: string;
  thumb: string;

  created_at: string;
  updated_at: string;
};
